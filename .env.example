# PostgreSQL Database Configuration
# Required for storing predictions
# Format: postgresql://username:password@hostname:port/database_name
DATABASE_URL=postgresql://user:password@localhost:5432/midopen

# For Render.com deployment:
# Render automatically provides this environment variable when you create a PostgreSQL service
# and link it to your web service. The format will be:
# DATABASE_URL=postgresql://username:password@hostname.region.render.com:5432/database_name
